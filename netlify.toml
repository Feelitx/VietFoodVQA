[build]
  # Provide the build command for your project
  command = "bun run build"
  
  # The directory containing your deployed assets
  publish = "dist"

[context.production.environment]
  # Tell Netlify to use Bun
  BUN_VERSION = "1.0.0"
  NODE_VERSION = "22"
  SECRETS_SCAN_ENABLED = "false"
